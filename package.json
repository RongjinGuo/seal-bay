{
  "name": "seal-bay",
  "version": "1.0.0",
  "description": "A touch-first Three.js seal feeding game with original Blender models",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test:browser": "node tests/browser-game.mjs && node tests/browser-audio.mjs && node tests/browser-beach.mjs && node tests/browser-petting.mjs && node tests/browser-roaming.mjs"
  },
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^8.2.2"
  },
  "private": true
}
